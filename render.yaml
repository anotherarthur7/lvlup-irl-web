# render.yaml
services:
  - type: web
    name: lvlup-irl-web
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 20